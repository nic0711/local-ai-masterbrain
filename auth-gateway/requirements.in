# Quelle fuer requirements.lock.txt (pip-compile --generate-hashes requirements.in).
# docker bleibt Runtime-Abhaengigkeit: Service-Control (service_control(),
# service_logs(), run_macro()) nutzt den Docker SDK aktiv, siehe
# docs/planning/security-exceptions/auth-gateway-docker-socket.yml.
# PyJWT und python-json-logger direkt hier gepinnt, weil masterbrain-common
# als Wheel mit --no-deps installiert wird - seine Laufzeit-Deps muessen
# deshalb ueber diesen Hash-Lock aufgeloest werden, nicht ueber das Wheel.
flask==3.1.3
gunicorn==26.0.0
supabase==2.31.0
flask-limiter==4.1.1
PyJWT==2.13.0
python-json-logger==4.1.0
docker>=7.2.0
