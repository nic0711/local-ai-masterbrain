# OCR Service (TrOCR / Surya / Tesseract)
{$OCR_HOSTNAME:ocr.invalid} {
    import protected
    reverse_proxy ocr-service:8002 {
        transport http {
            response_header_timeout 120s
        }
    }
}
