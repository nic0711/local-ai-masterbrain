# Hermes Agent Dashboard – On-Demand (Profil: optional)
{$HERMES_HOSTNAME:hermes.invalid} {
    import protected
    reverse_proxy hermes-dashboard:9119 {
        transport http {
            response_header_timeout 60s
        }
    }
}
