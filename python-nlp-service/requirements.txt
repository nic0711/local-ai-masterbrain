# requirements.txt

# Web Framework
flask==3.1.3
werkzeug==3.1.8

# Production WSGI Server
gunicorn==25.1.0

# NLP Core
# spacy 3.8.x ist kompatibel mit NumPy 2.x
# Bewusst auf 3.8.13 gepinnt (nicht das neuere 3.8.14): 3.8.14 wurde von
# Dependabot vorgeschlagen, bevor der separate Python-3.14-Bump (siehe
# Dockerfile) gemergt war. spacy 3.8.14 hat auf PyPI weder ein cp314-Wheel
# noch ein Sdist (verifiziert via PyPI JSON API) - unter Python 3.14
# schlaegt `pip install` fehl ("No matching distribution"). 3.8.13 hat
# sowohl cp314-Wheels als auch ein Sdist und ist damit kompatibel; kein
# CVE/Security-Advisory fuer spacy betrifft diesen Rueckschritt (GitHub
# Advisory DB liefert 0 Treffer fuer "spacy" zum Zeitpunkt dieses Fixes).
spacy==3.8.13
spacy-lookups-data==1.0.5

# HTTP Client (für Ollama-Calls und Health Checks)
requests==2.33.0

# Logging und Monitoring
python-json-logger==4.1.0

# Performance und Utils
# NumPy 2.x: Breaking Changes gegenüber 1.x, aber spacy 3.8.x ist kompatibel
numpy==2.5.1

# Neo4j Graph-Datenbank (für GraphRAG)
neo4j==5.27.0

# Dokument- und PDF-Verarbeitung
PyMuPDF==1.28.0           # PDF-Text-Extraktion und PDF→PNG (fitz)
Pillow==12.3.0            # Bildverarbeitung für OCR-Vorbereitung

# System Dependencies werden im Dockerfile installiert:
# - curl (für HEALTHCHECK)
# - build-essential (für manche Python-Pakete, nur im Builder-Stage)
